Bug ID,Test Scenario,Root Cause,Impact,Resolution Applied,Verification Status
BUG-01,TC06 / TC07 (Pedestrian Request),Non-clock-aligned #10 stimulus pulse deasserted before active green countdown timer completed.,Testbench deadlock: wait(current_state == 4'd6) hung indefinitely at ~4.85 ms.,"Synchronized assertion to @(posedge clk), maintained request until granted, and added a bounded 200-cycle loop timeout.",CLOSED / VERIFIED
BUG-02,Check 3 (Emergency Monitor),Boolean assertion logic used !(NS_red_A || EW_red_A || NS_red_B || EW_red_B).,Inverted logical mask allowed hazardous single-junction green lights during active emergency overrides without failing.,Replaced || with strict conjunction: !(NS_red_A && EW_red_A && NS_red_B && EW_red_B).,CLOSED / VERIFIED
BUG-03,TC12 (Emergency Recovery),State inspection sampled on the immediate clock edge where emergency_override dropped to 0.,Race condition: Sampled stale register states prior to non-blocking assignment (<=) resolution.,"Added 1 clock cycle recovery latency plus #1 delta delay before asserting state membership in {State 2, State 5}.",CLOSED / VERIFIED
BUG-04,TC09 (Starvation Test),Stimulus loop used self-referencing single-bit modulo: ped_request_A <= (ped_request_A % 2 == 0).,Alternating 1-bit toggle instead of testing true coprime sequence intervals.,Updated modulo reference to the loop induction variable: (ped_iteration % 2 == 0).,CLOSED / VERIFIED
BUG-05,TC05 (Max Clamp Test),Typo in stimulus assignment assigned traffic_density_A = 3'd7 instead of Junction B.,Junction B clamp logic remained unexercised under peak load.,Corrected assignment to traffic_density_B = 3'd7.,CLOSED / VERIFIED
